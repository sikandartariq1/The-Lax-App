<tr>
  <td><%= feedback.feedback_entity(current_entity).full_name %></td>
  <td><%= feedback.created_at.strftime("%m-%d-%Y") %></td>
  <td><%= feedback.comments.truncate(20) %></td>
  <td><%= link_to 'Show', [current_entity, feedback] %></td>
</tr>
